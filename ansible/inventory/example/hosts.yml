---
# Copy this directory to ansible/inventory/local (gitignored) and fill in your
# real addresses. The values here are RFC 5737 documentation ranges.
mother_ticker:
  hosts:
    ntp-main:
      ansible_host: 192.0.2.20
    ntp-malware:
      ansible_host: 198.51.100.20
