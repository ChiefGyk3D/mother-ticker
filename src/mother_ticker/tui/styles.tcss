/* Mother Ticker TUI stylesheet.
   Colours are the terminal's ANSI palette so the Linux console (16 colours,
   TERM=linux) and an SSH session look the same. */

Screen {
    layout: vertical;
}

#banner {
    height: 3;
    content-align: center middle;
    text-style: bold;
    background: ansi_green;
    color: ansi_black;
}
#banner.warning {
    background: ansi_yellow;
    color: ansi_black;
}
#banner.critical {
    background: ansi_red;
    color: ansi_white;
}
#banner.critical.flash {
    background: ansi_white;
    color: ansi_red;
}
#banner.warning.flash {
    background: ansi_black;
    color: ansi_yellow;
}

#clock {
    width: 100%;
    height: 5;
    content-align: center middle;
    text-align: center;
    margin-top: 1;
    color: ansi_bright_cyan;
}
#clock-sub {
    width: 100%;
    text-align: center;
    color: ansi_bright_black;
}

#panels {
    height: 1fr;
    margin: 1 2 0 2;
    grid-size: 2;
    grid-gutter: 0 2;
}
.panel {
    height: 1fr;
    border: solid ansi_bright_black;
    padding: 0 0 0 1;
}
.panel-icon {
    width: 17;
    height: 1fr;
    content-align: left middle;
    color: ansi_bright_cyan;
}
.panel-text {
    width: 1fr;
    height: 1fr;
    content-align: left middle;
    padding-left: 1;
}
.panel.warning {
    border: solid ansi_yellow;
}
.panel.warning .panel-icon {
    color: ansi_yellow;
}
.panel.critical {
    border: solid ansi_red;
    color: ansi_red;
    text-style: bold;
}
.panel.critical .panel-icon {
    color: ansi_red;
}

#about-title {
    width: 100%;
    height: 4;
    text-align: center;
    margin-top: 1;
    color: ansi_bright_cyan;
    text-style: bold;
}
#about-tagline {
    width: 100%;
    text-align: center;
    color: ansi_bright_black;
}
#about-body {
    height: 1fr;
    padding: 1 2;
}
#about-logo {
    width: 50;
    height: 15;
    color: ansi_bright_cyan;
}
#about-text {
    width: 1fr;
    padding: 1 0 0 2;
}

#dash-footer {
    dock: bottom;
    height: 1;
    color: ansi_bright_black;
    padding: 0 1;
}
#footer-left {
    width: 1fr;
}
#footer-right {
    width: auto;
    text-align: right;
}

.title {
    height: 1;
    text-style: bold;
    padding: 0 1;
    background: ansi_blue;
    color: ansi_white;
}
.hint {
    dock: bottom;
    height: 1;
    color: ansi_bright_black;
    padding: 0 1;
}
.pre {
    padding: 0 1;
}
.status-line {
    padding: 0 1;
    height: auto;
}

OptionList {
    margin: 1 2;
    height: auto;
    max-height: 100%;
}
#menu-body {
    height: 1fr;
    align: center middle;
    padding: 0 2;
}
#menu-logo {
    width: 50;
    height: 15;
    color: ansi_bright_cyan;
}
#menu-column {
    width: 1fr;
    height: auto;
}
#menu-heading {
    padding: 0 2;
    text-style: bold;
    color: ansi_bright_cyan;
}
#menu {
    margin: 0 2;
}
DataTable {
    margin: 0 1;
    height: 1fr;
}
Log {
    margin: 0 1;
    height: 1fr;
}

ConfirmScreen {
    align: center middle;
}
#confirm-box {
    width: 60;
    height: auto;
    border: thick ansi_red;
    padding: 1 2;
}
#confirm-box Label {
    width: 100%;
    margin-bottom: 1;
}
#confirm-buttons {
    height: 3;
    align: center middle;
}
#confirm-buttons Button {
    margin: 0 2;
}
